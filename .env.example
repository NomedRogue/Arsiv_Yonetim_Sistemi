# JWT Secret Key for Authentication
# IMPORTANT: Generate a strong random secret (minimum 32 characters)
# You can generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secret-key-here-change-this-in-production-minimum-32-chars

# Database Path (optional, defaults to backend/arsiv.db in development)
# DB_PATH=

# User Data Path (optional, defaults to backend/ in development)
# USER_DATA_PATH=

# Node Environment
NODE_ENV=development

# Server Port
PORT=3001
